# PaymentApplicationFrontend

To start the local server, run:

ng serve

Once the server is running, open your browser and navigate to `http://localhost:4200/`.

Make sure the backend application is running simultaneously